import { Component, signal } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { Router, RouterLink } from '@angular/router';
import { AuthService } from '../core/services/auth.service';
import { ToastService } from '../core/services/toast.service';
@Component({imports:[ReactiveFormsModule,RouterLink],template:`<section class="auth-page"><div class="auth-art"><span class="eyebrow">WELCOME BACK</span><h1>Your next favourite meal is waiting.</h1><div>🍜</div></div><div class="auth-panel"><form class="form-card" [formGroup]="form" (ngSubmit)="submit()"><span class="eyebrow">SIGN IN</span><h2>Good to see you</h2><p>Use a demo account or your own credentials.</p><label>Email address<input type="email" formControlName="email" placeholder="you@example.com"></label>@if(form.controls.email.touched&&form.controls.email.invalid){<small class="error">Enter a valid email.</small>}<label>Password<input type="password" formControlName="password" placeholder="At least 8 characters"></label><div class="between"><label class="check"><input type="checkbox"> Remember me</label><a routerLink="/forgot-password">Forgot password?</a></div><button class="btn full" [disabled]="form.invalid||loading()">{{loading()?'Signing in…':'Sign in'}}</button><p class="center">New here? <a routerLink="/register">Create an account</a></p><div class="demo-hint"><b>Demo:</b> customer@example.com / FoodFlow&#64;123</div></form></div></section>`})
export class LoginComponent{loading=signal(false);form=this.fb.nonNullable.group({email:['customer@example.com',[Validators.required,Validators.email]],password:['FoodFlow@123',[Validators.required,Validators.minLength(8)]]});constructor(private fb:FormBuilder,private auth:AuthService,private router:Router,private toast:ToastService){}submit(){if(this.form.invalid)return;this.loading.set(true);this.auth.login(this.form.getRawValue()).subscribe({next:()=>{this.toast.show('Login successful');void this.router.navigateByUrl(this.auth.dashboardPath());},error:()=>this.loading.set(false)});}}
