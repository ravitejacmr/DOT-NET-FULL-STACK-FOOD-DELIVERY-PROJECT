import { Component } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { AuthService } from '../core/services/auth.service';
import { ToastService } from '../core/services/toast.service';
@Component({imports:[ReactiveFormsModule],template:`<section class="page-hero small"><div class="container"><span class="eyebrow">YOUR ACCOUNT</span><h1>Profile</h1></div></section><section class="section narrow"><form class="panel form-card wide" [formGroup]="form" (ngSubmit)="save()"><div class="avatar">{{initials}}</div><div class="form-grid"><label>First name<input formControlName="first_name"></label><label>Last name<input formControlName="last_name"></label><label class="span-2">Email<input formControlName="email" readonly></label><label class="span-2">Phone<input formControlName="phone"></label></div><button class="btn" [disabled]="form.invalid">Save changes</button></form></section>`})
export class ProfileComponent{user=this.auth.user()!;initials=`${this.user.first_name[0]||''}${this.user.last_name[0]||''}`;form=this.fb.nonNullable.group({first_name:[this.user.first_name,Validators.required],last_name:[this.user.last_name,Validators.required],email:[this.user.email],phone:[this.user.phone,Validators.required]});constructor(private fb:FormBuilder,private auth:AuthService,private toast:ToastService){}save(){this.auth.updateProfile(this.form.getRawValue()).subscribe(()=>this.toast.show('Profile updated'));}}
