import { Component, OnInit, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { Router, RouterLink } from '@angular/router';
import { Address, Cart, Order } from '../core/models/models';
import { ApiService } from '../core/services/api.service';
import { ToastService } from '../core/services/toast.service';
@Component({imports:[FormsModule,RouterLink],template:`<section class="page-hero small"><div class="container"><span class="eyebrow">ALMOST THERE</span><h1>Checkout</h1></div></section><section class="section container"><div class="checkout-grid"><div><div class="panel"><div class="section-head"><h2>Delivery address</h2><a routerLink="/customer/addresses">Add address</a></div>@for(a of addresses();track a.id){<label class="address-choice" [class.selected]="addressId===a.id"><input type="radio" name="address" [value]="a.id" [(ngModel)]="addressId"><div><b>{{a.full_name}} · {{a.address_type}}</b><p>{{a.house_number}}, {{a.street}}, {{a.city}} {{a.postal_code}}</p></div></label>}@empty{<div class="empty compact"><h3>No delivery address</h3><p>Add an address before placing your order.</p><a routerLink="/customer/addresses" class="btn small">Add delivery address</a></div>}</div><div class="panel"><h2>Payment method</h2><label class="address-choice" [class.selected]="payment==='cod'"><input type="radio" value="cod" [(ngModel)]="payment" name="payment"><div><b>Cash on Delivery</b><p>Pay when your food arrives.</p></div></label><label class="address-choice" [class.selected]="payment==='online'"><input type="radio" value="online" [(ngModel)]="payment" name="payment"><div><b>Mock Online Payment</b><p>Use the secure payment simulator.</p></div></label><label>Notes for the restaurant<textarea [(ngModel)]="notes" placeholder="Less spicy, no cutlery…"></textarea></label></div></div>@if(cart();as c){<aside class="summary"><h2>Payment details</h2><div><span>Subtotal</span><b>₹{{c.subtotal}}</b></div><div><span>Tax</span><b>₹{{c.tax}}</b></div><div><span>Delivery</span><b>₹{{c.delivery_fee}}</b></div><div class="discount"><span>Discount</span><b>−₹{{c.discount}}</b></div><div class="total"><span>Grand total</span><b>₹{{c.grand_total}}</b></div><button class="btn full" [disabled]="submitting()" (click)="place()">{{!addressId?'Add delivery address to continue':submitting()?'Placing order…':'Place order'}}</button></aside>}</div></section>`})
export class CheckoutComponent implements OnInit{cart=signal<Cart|null>(null);addresses=signal<Address[]>([]);addressId?:number;payment='cod';notes='';submitting=signal(false);constructor(private api:ApiService,private router:Router,private toast:ToastService){}ngOnInit(){this.api.cart().subscribe(v=>this.cart.set(v));this.api.addresses().subscribe(v=>{this.addresses.set(v.results);this.addressId=v.results.find(a=>a.is_default)?.id||v.results[0]?.id;});}place(){if(!this.addressId){this.toast.show('Add a delivery address to continue.','info');void this.router.navigateByUrl('/customer/addresses');return;}this.submitting.set(true);this.api.placeOrder(this.addressId,this.payment,this.notes).subscribe({next:(o:Order)=>{this.toast.show('Order placed successfully');void this.router.navigate([this.payment==='online'?'/payment':'/order-confirmation',o.id]);},error:()=>this.submitting.set(false)});}}
