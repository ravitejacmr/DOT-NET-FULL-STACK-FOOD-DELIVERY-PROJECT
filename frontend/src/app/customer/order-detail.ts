import { Component, OnInit, signal } from '@angular/core';
import { DatePipe } from '@angular/common';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { Order } from '../core/models/models';
import { ApiService } from '../core/services/api.service';
import { ToastService } from '../core/services/toast.service';
@Component({imports:[DatePipe,RouterLink],template:`@if(order();as o){<section class="page-hero small"><div class="container"><span class="eyebrow">{{confirmation?'ORDER CONFIRMED':'ORDER DETAILS'}}</span><h1>{{confirmation?'Thank you!':'Track your order'}}</h1><p>{{o.order_number}} · Placed {{o.created_at|date:'medium'}}</p></div></section><section class="section container"><div class="detail-grid"><div><div class="panel"><div class="section-head"><h2>{{o.restaurant_name}}</h2><span class="badge" [class]="o.status">{{o.status_label}}</span></div><div class="timeline">@for(step of steps;track step.status;let i=$index){<div [class.done]="stepDone(step.status,o)" [class.current]="o.status===step.status"><i>{{stepDone(step.status,o)?'✓':i+1}}</i><div><b>{{step.label}}</b><small>{{eventDate(step.status,o)|date:'shortTime'}}</small></div></div>}</div></div><div class="panel"><h2>Items</h2>@for(item of o.items;track item.id){<div class="summary-line"><span>{{item.quantity}} × {{item.food_name}}</span><b>₹{{item.subtotal}}</b></div>}</div></div><aside class="summary"><h2>Order summary</h2><div><span>Subtotal</span><b>₹{{o.subtotal}}</b></div><div><span>Tax</span><b>₹{{o.tax}}</b></div><div><span>Delivery</span><b>₹{{o.delivery_fee}}</b></div><div><span>Discount</span><b>−₹{{o.discount}}</b></div><div class="total"><span>Total</span><b>₹{{o.grand_total}}</b></div><p><b>Payment:</b> {{o.payment_method==='cod'?'Cash on delivery':'Online'}} · {{o.payment_status}}</p><p><b>Deliver to:</b><br>{{o.delivery_address_snapshot['house_number']}}, {{o.delivery_address_snapshot['street']}}, {{o.delivery_address_snapshot['city']}}</p>@if(['pending','confirmed','accepted'].includes(o.status)){<button class="btn danger full" (click)="cancel()">Cancel order</button>}<a routerLink="/restaurants" class="btn ghost full">Order more food</a></aside></div></section>}`})
export class OrderDetailComponent implements OnInit{order=signal<Order|null>(null);confirmation=false;steps=[{status:'pending',label:'Order placed'},{status:'accepted',label:'Restaurant accepted'},{status:'preparing',label:'Preparing your food'},{status:'ready',label:'Ready for pickup'},{status:'assigned',label:'Rider assigned'},{status:'picked_up',label:'Picked up'},{status:'on_the_way',label:'On the way'},{status:'delivered',label:'Delivered'}];constructor(private route:ActivatedRoute,private api:ApiService,private toast:ToastService){}ngOnInit(){this.confirmation=this.route.snapshot.url[0]?.path==='order-confirmation';this.load();}load(){this.api.order(Number(this.route.snapshot.paramMap.get('id'))).subscribe(v=>this.order.set(v));}stepDone(status:string,o:Order){const current=this.steps.findIndex(s=>s.status===o.status);return this.steps.findIndex(s=>s.status===status)<=current&&!['cancelled','rejected'].includes(o.status);}eventDate(status:string,o:Order){return o.status_events.find(e=>e.status===status)?.created_at||'';}cancel(){const o=this.order();if(o&&confirm('Cancel this order?'))this.api.cancelOrder(o.id).subscribe(v=>{this.order.set(v);this.toast.show('Order cancelled','info');});}}
