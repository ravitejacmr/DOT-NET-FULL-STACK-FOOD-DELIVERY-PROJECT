import { Component, OnInit, signal } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { Address } from '../core/models/models';
import { ApiService } from '../core/services/api.service';
import { ToastService } from '../core/services/toast.service';
@Component({imports:[ReactiveFormsModule],template:`<section class="page-hero small"><div class="container"><span class="eyebrow">SAVED PLACES</span><h1>Delivery addresses</h1></div></section><section class="section container"><div class="detail-grid"><div class="panel"><h2>Your addresses</h2>@for(a of addresses();track a.id){<article class="address-card"><span>{{a.address_type==='home'?'⌂':'⌖'}}</span><div class="grow"><b>{{a.full_name}} @if(a.is_default){<em>Default</em>}</b><p>{{a.house_number}}, {{a.street}}, {{a.city}} {{a.postal_code}}</p></div>@if(!a.is_default){<button class="link-button" (click)="setDefault(a.id)">Set default</button>}<button class="icon-button" (click)="remove(a.id)">×</button></article>}@empty{<div class="empty compact">No addresses saved.</div>}</div><form class="panel" [formGroup]="form" (ngSubmit)="save()"><h2>Add a new address</h2><div class="form-grid">@for(f of fields;track f.key){<label [class.span-2]="f.wide">{{f.label}}<input [formControlName]="f.key"></label>}<label>Type<select formControlName="address_type"><option value="home">Home</option><option value="office">Office</option><option value="other">Other</option></select></label><label class="check"><input type="checkbox" formControlName="is_default"> Set as default</label></div><button class="btn" [disabled]="form.invalid">Save address</button></form></div></section>`})
export class AddressesComponent implements OnInit{addresses=signal<Address[]>([]);fields=[{key:'full_name',label:'Full name',wide:false},{key:'phone',label:'Phone',wide:false},{key:'house_number',label:'House / flat',wide:false},{key:'street',label:'Street',wide:false},{key:'landmark',label:'Landmark',wide:true},{key:'city',label:'City',wide:false},{key:'state',label:'State',wide:false},{key:'postal_code',label:'Postal code',wide:false}];form=this.fb.nonNullable.group({full_name:['',Validators.required],phone:['',Validators.required],house_number:['',Validators.required],street:['',Validators.required],landmark:[''],city:['',Validators.required],state:['',Validators.required],postal_code:['',Validators.required],address_type:['home' as const],is_default:[false]});constructor(private fb:FormBuilder,private api:ApiService,private toast:ToastService){}ngOnInit(){this.load();}load(){this.api.addresses().subscribe(v=>this.addresses.set(v.results));}save(){this.api.createAddress(this.form.getRawValue()).subscribe(()=>{this.toast.show('Address saved');this.form.reset();this.load();});}remove(id:number){if(confirm('Delete this address?'))this.api.deleteAddress(id).subscribe(()=>this.load());}setDefault(id:number){this.api.setDefaultAddress(id).subscribe(()=>this.load());}}
