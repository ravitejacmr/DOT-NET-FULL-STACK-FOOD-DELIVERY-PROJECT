import { Component, OnInit, signal } from '@angular/core';
import { RouterLink } from '@angular/router';
import { ApiService } from '../core/services/api.service';
import { AuthService } from '../core/services/auth.service';
@Component({imports:[RouterLink],template:`<section class="dashboard-page"><div class="container"><div class="welcome"><div><span class="eyebrow">RESTAURANT COMMAND CENTRE</span><h1>Welcome back, {{auth.user()?.first_name}}</h1><p>Keep service moving and your customers smiling.</p></div><a routerLink="/owner/menu" class="btn">Manage menu</a></div><div class="metric-grid">@for(m of metrics;track m.key){<article><span>{{m.label}}</span><b>{{value(m.key,m.money)}}</b><small>{{m.note}}</small></article>}</div><div class="dashboard-columns"><div class="panel"><div class="section-head"><h2>Top selling items</h2></div>@for(item of list('top_selling_items');track item['food_name']){<div class="summary-line"><span>{{item['food_name']}}</span><b>{{item['quantity']}} sold</b></div>}@empty{<div class="empty compact">Sales insights appear after orders.</div>}</div><div class="panel"><h2>Quick actions</h2><a routerLink="/owner/orders" class="quick-link">📋 <span><b>Manage orders</b><small>Accept and move orders forward</small></span>→</a><a routerLink="/owner/menu" class="quick-link">🍽️ <span><b>Menu items</b><small>Prices, offers and availability</small></span>→</a><a routerLink="/owner/reviews" class="quick-link">★ <span><b>Customer reviews</b><small>See what diners are saying</small></span>→</a></div></div></div></section>`})
export class OwnerDashboardComponent implements OnInit{data=signal<Record<string,unknown>>({});metrics=[{key:'total_orders',label:'Total orders',note:'All time'},{key:'today_orders',label:"Today's orders",note:'Since midnight'},{key:'preparing_orders',label:'Preparing',note:'In the kitchen'},{key:'revenue',label:'Revenue',note:'Delivered orders',money:true}];constructor(private api:ApiService,public auth:AuthService){}ngOnInit(){this.api.dashboard().subscribe(v=>this.data.set(v));}value(key:string,money?:boolean){return `${money?'₹':''}${this.data()[key]??0}`;}list(key:string):Record<string,unknown>[]{return (this.data()[key] as Record<string,unknown>[])||[];}}
