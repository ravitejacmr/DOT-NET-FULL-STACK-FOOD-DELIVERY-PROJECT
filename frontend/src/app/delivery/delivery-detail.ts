import { Component, OnInit, signal } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { Order } from '../core/models/models';
import { ApiService } from '../core/services/api.service';
import { ToastService } from '../core/services/toast.service';
@Component({template:`@if(order();as o){<section class="page-hero small"><div class="container"><span class="eyebrow">ACTIVE DELIVERY</span><h1>{{o.order_number}}</h1></div></section><section class="section narrow"><div class="panel"><h2>Pickup</h2><p><b>{{o.restaurant_name}}</b></p><hr><h2>Drop-off</h2><p>{{o.delivery_address_snapshot['full_name']}} · {{o.delivery_address_snapshot['phone']}}</p><p>{{o.delivery_address_snapshot['house_number']}}, {{o.delivery_address_snapshot['street']}}, {{o.delivery_address_snapshot['city']}}</p><div class="action-stack">@if(o.status==='assigned'){<button class="btn" (click)="move('picked_up')">Confirm pickup</button>}@if(o.status==='picked_up'){<button class="btn" (click)="move('on_the_way')">Start delivery</button>}@if(o.status==='on_the_way'){<button class="btn" (click)="move('delivered')">Mark delivered</button>}</div></div></section>}`})
export class DeliveryDetailComponent implements OnInit{order=signal<Order|null>(null);id=0;constructor(private route:ActivatedRoute,private api:ApiService,private toast:ToastService){}ngOnInit(){this.id=Number(this.route.snapshot.paramMap.get('id'));this.load();}load(){this.api.order(this.id).subscribe(v=>this.order.set(v));}move(status:string){this.api.transitionOrder(this.id,status).subscribe(v=>{this.order.set(v);this.toast.show(`Delivery marked ${status.replace('_',' ')}`);});}}
