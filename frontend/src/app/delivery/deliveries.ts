import { Component, OnInit, signal, Input } from '@angular/core';
import { DatePipe } from '@angular/common';
import { RouterLink } from '@angular/router';
import { Order } from '../core/models/models';
import { ApiService } from '../core/services/api.service';
import { ToastService } from '../core/services/toast.service';
@Component({imports:[DatePipe,RouterLink],template:`<section class="page-hero small"><div class="container"><span class="eyebrow">{{history?'DELIVERY HISTORY':'PICK YOUR NEXT RUN'}}</span><h1>{{history?'Completed deliveries':'Available deliveries'}}</h1></div></section><section class="section container"><div class="delivery-list">@for(o of orders();track o.id){<article class="delivery-card"><div class="route-line"><i>R</i><span></span><i>D</i></div><div class="grow"><b>{{o.restaurant_name}}</b><p>{{o.delivery_address_snapshot['street']}}, {{o.delivery_address_snapshot['city']}}</p><small>{{o.order_number}} · {{o.created_at|date:'short'}}</small></div><div><b>₹{{o.delivery_fee}}</b><small>Delivery earning</small></div>@if(!history){<button class="btn" (click)="accept(o)">Accept</button>}@else{<a class="btn ghost" [routerLink]="['/delivery/orders',o.id]">Details</a>}</article>}@empty{<div class="empty panel">🛵<h2>{{history?'No completed deliveries':'No deliveries waiting'}}</h2><p>Check back shortly for new opportunities.</p></div>}</div></section>`})
export class DeliveriesComponent implements OnInit{@Input() history=false;orders=signal<Order[]>([]);constructor(private api:ApiService,private toast:ToastService){}ngOnInit(){this.load();}load(){if(this.history)this.api.orders({status:'delivered'}).subscribe(v=>this.orders.set(v.results));else this.api.availableDeliveries().subscribe(v=>this.orders.set(v.results));}accept(o:Order){this.api.acceptDelivery(o.id).subscribe(()=>{this.toast.show('Delivery accepted');this.load();});}}
